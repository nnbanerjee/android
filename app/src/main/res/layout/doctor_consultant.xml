<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/doctor_name_text"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:padding="5dp"
        android:text="Name:"
        style="@style/body2"
    />

    <Spinner
        android:id="@+id/doctor_list"
        android:layout_width="match_parent"
        android:layout_height="30dp"
        android:layout_marginRight="15dp"
        android:layout_toRightOf="@+id/doctor_name_text"
        android:background="@drawable/black_border"></Spinner>
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/doctor_list"
        android:layout_marginTop="10dp"
        android:visibility="gone"
        android:id="@+id/add_appointment_layout" >

    </FrameLayout>

    <ListView
        android:id="@+id/clinic_doctor_consult_list"
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:layout_below="@+id/doctor_list"
        android:layout_marginTop="10dp"
        android:background="#ffffff"
        android:padding="5dp"></ListView>

</RelativeLayout>